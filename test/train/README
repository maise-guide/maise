A neural network stratified training job for Cu-Ag using a small Cu-Ag dataset. Previously trained Cu.dat and Ag.dat elemental models are provided in NNET/ 

Run 'maise' to train the neural network model. The run will produce the follwing files in NNET/ : model, energy_errors.dat, error.dat, and out.dat .

For comparison, the corresponding reference output files are provided in the ref/ directory. 
